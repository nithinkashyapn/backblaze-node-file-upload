# Backblaze Node File Upload

> Update backblaze `account_id`, `application_key` & `bucket_name` in app.js

## Usage
`npm i && node app`

##### Note: Always store files to be uploaded in `uploads` folder`
